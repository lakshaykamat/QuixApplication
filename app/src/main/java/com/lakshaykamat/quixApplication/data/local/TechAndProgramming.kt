package com.lakshaykamat.quixApplication.data.local

import com.lakshaykamat.quixApplication.data.Options
import com.lakshaykamat.quixApplication.data.Quiz

val techAndProgrammingQuestionsData = listOf(
    Quiz(
        question = "What does HTML stand for?",
        options = listOf(
            Options(text = "Hyper Text Markup Language", isRight = true),
            Options(text = "High Tech Multi Language", isRight = false),
            Options(text = "Hyperlink and Text Markup Language", isRight = false),
            Options(text = "Home Tool Markup Language", isRight = false)
        )
    ),
    Quiz(
        question = "In programming, what does API stand for?",
        options = listOf(
            Options(text = "Application Programming Interface", isRight = true),
            Options(text = "Advanced Programming Interface", isRight = false),
            Options(text = "Automated Program Integration", isRight = false),
            Options(text = "Application Program Interface", isRight = false)
        )
    ),
    Quiz(
        question = "What is the primary purpose of CSS?",
        options = listOf(
            Options(text = "Database Management", isRight = false),
            Options(text = "Server-side scripting", isRight = false),
            Options(text = "Styling and formatting web pages", isRight = true),
            Options(text = "Algorithm development", isRight = false)
        )
    ),
    Quiz(
        question = "Which programming language is known for its use in artificial intelligence?",
        options = listOf(
            Options(text = "Java", isRight = false),
            Options(text = "Python", isRight = true),
            Options(text = "C++", isRight = false),
            Options(text = "Ruby", isRight = false)
        )
    ),
    Quiz(
        question = "What is the purpose of the 'git clone' command?",
        options = listOf(
            Options(text = "Create a new repository", isRight = false),
            Options(text = "Make a copy of a repository", isRight = true),
            Options(text = "Delete a repository", isRight = false),
            Options(text = "Move a repository to a new location", isRight = false)
        )
    ),
    Quiz(
        question = "Which data structure follows the Last In, First Out (LIFO) principle?",
        options = listOf(
            Options(text = "Queue", isRight = false),
            Options(text = "Stack", isRight = true),
            Options(text = "Linked List", isRight = false),
            Options(text = "Tree", isRight = false)
        )
    ),
    Quiz(
        question = "What does the acronym SQL stand for?",
        options = listOf(
            Options(text = "Structured Question Language", isRight = false),
            Options(text = "System Query Language", isRight = false),
            Options(text = "Structured Query Language", isRight = true),
            Options(text = "Simple Query Language", isRight = false)
        )
    ),
    Quiz(
        question = "Which programming language is often used for developing mobile applications?",
        options = listOf(
            Options(text = "C#", isRight = false),
            Options(text = "Swift", isRight = true),
            Options(text = "Java", isRight = false),
            Options(text = "Python", isRight = false)
        )
    ),
    Quiz(
        question = "What is the purpose of the 'try' and 'catch' blocks in exception handling?",
        options = listOf(
            Options(text = "Terminate the program", isRight = false),
            Options(text = "Define a function", isRight = false),
            Options(text = "Handle and recover from errors", isRight = true),
            Options(text = "Declare variables", isRight = false)
        )
    ),
    Quiz(
        question = "What does the acronym URL stand for?",
        options = listOf(
            Options(text = "Uniform Resource Locator", isRight = true),
            Options(text = "Universal Reference Language", isRight = false),
            Options(text = "Unified Resource Listing", isRight = false),
            Options(text = "User Registration Link", isRight = false)
        )
    ),
    Quiz(
        question = "In object-oriented programming, what is encapsulation?",
        options = listOf(
            Options(text = "Breaking down a problem into smaller parts", isRight = false),
            Options(text = "Combining data and methods into a single unit", isRight = true),
            Options(text = "Creating multiple instances of a class", isRight = false),
            Options(text = "Handling errors in code", isRight = false)
        )
    ),
    Quiz(
        question = "Which of the following is a version control system?",
        options = listOf(
            Options(text = "Git", isRight = true),
            Options(text = "Java", isRight = false),
            Options(text = "HTML", isRight = false),
            Options(text = "CSS", isRight = false)
        )
    ),
    Quiz(
        question = "What is the purpose of the 'npm' command in JavaScript development?",
        options = listOf(
            Options(text = "Node Package Manager", isRight = true),
            Options(text = "New Project Manager", isRight = false),
            Options(text = "Node Project Manager", isRight = false),
            Options(text = "Nesting Package Manager", isRight = false)
        )
    ),
    Quiz(
        question = "In the context of databases, what does CRUD stand for?",
        options = listOf(
            Options(text = "Create, Retrieve, Update, Delete", isRight = true),
            Options(text = "Compile, Run, Upload, Deploy", isRight = false),
            Options(text = "Copy, Replace, Undo, Debug", isRight = false),
            Options(text = "Calculate, Report, Understand, Document", isRight = false)
        )
    ),
    Quiz(
        question = "Which programming language is known for its use in web development?",
        options = listOf(
            Options(text = "Swift", isRight = false),
            Options(text = "Ruby", isRight = false),
            Options(text = "JavaScript", isRight = true),
            Options(text = "C++", isRight = false)
        )
    ),
    Quiz(
        question = "What does the acronym IDE stand for in software development?",
        options = listOf(
            Options(text = "Integrated Development Environment", isRight = true),
            Options(text = "Interactive Design Environment", isRight = false),
            Options(text = "Internet Development Engine", isRight = false),
            Options(text = "Intelligent Data Exchange", isRight = false)
        )
    ),
    Quiz(
        question = "What is the purpose of the 'docker' command in containerization?",
        options = listOf(
            Options(text = "Deploy a web server", isRight = false),
            Options(text = "Build a virtual machine", isRight = false),
            Options(text = "Manage and deploy containers", isRight = true),
            Options(text = "Create a database", isRight = false)
        )
    ),
    Quiz(
        question = "In Python, what is the purpose of the 'lambda' keyword?",
        options = listOf(
            Options(text = "Declare a constant", isRight = false),
            Options(text = "Define a class", isRight = false),
            Options(text = "Create anonymous functions", isRight = true),
            Options(text = "Import a module", isRight = false)
        )
    ),
    Quiz(
        question = "What is the purpose of the 'REST' architecture in web development?",
        options = listOf(
            Options(text = "Representational State Transfer", isRight = true),
            Options(text = "Remote Execution and Server Transfer", isRight = false),
            Options(text = "Randomized Element Sorting Technique", isRight = false),
            Options(text = "Reactive Event Synchronization Technology", isRight = false)
        )
    ),
    Quiz(
        question = "What does the acronym SQL stand for?",
        options = listOf(
            Options(text = "Structured Query Language", isRight = true),
            Options(text = "System Question Language", isRight = false),
            Options(text = "Simple Query Logic", isRight = false),
            Options(text = "Standard Query Level", isRight = false)
        )
    ),
    Quiz(
        question = "Which programming language is commonly used for developing Android applications?",
        options = listOf(
            Options(text = "Swift", isRight = false),
            Options(text = "Java", isRight = false),
            Options(text = "Kotlin", isRight = true),
            Options(text = "C#", isRight = false)
        )
    ),
    Quiz(
        question = "What is the purpose of the 'try', 'catch', and 'finally' blocks in exception handling?",
        options = listOf(
            Options(text = "Terminate the program", isRight = false),
            Options(text = "Define a function", isRight = false),
            Options(text = "Handle and recover from errors", isRight = true),
            Options(text = "Declare variables", isRight = false)
        )
    ),
    Quiz(
        question = "Which data structure is used to implement a Last In, First Out (LIFO) queue?",
        options = listOf(
            Options(text = "Queue", isRight = false),
            Options(text = "Stack", isRight = true),
            Options(text = "Linked List", isRight = false),
            Options(text = "Heap", isRight = false)
        )
    ),
    Quiz(
        question = "What is the purpose of the 'npm' command in JavaScript development?",
        options = listOf(
            Options(text = "Node Package Manager", isRight = true),
            Options(text = "New Project Manager", isRight = false),
            Options(text = "Node Project Manager", isRight = false),
            Options(text = "Nesting Package Manager", isRight = false)
        )
    ),
    Quiz(
        question = "Which programming paradigm does JavaScript support?",
        options = listOf(
            Options(text = "Procedural", isRight = false),
            Options(text = "Object-Oriented", isRight = false),
            Options(text = "Functional", isRight = false),
            Options(text = "All of the above", isRight = true)
        )
    ),
    Quiz(
        question = "What is the purpose of the 'git pull' command?",
        options = listOf(
            Options(text = "Upload changes to a remote repository", isRight = false),
            Options(text = "Retrieve changes from a remote repository", isRight = true),
            Options(text = "Create a new branch", isRight = false),
            Options(text = "Delete a branch", isRight = false)
        )
    ),
    Quiz(
        question = "In networking, what does the acronym DNS stand for?",
        options = listOf(
            Options(text = "Domain Name Server", isRight = false),
            Options(text = "Dynamic Network System", isRight = false),
            Options(text = "Data Network Security", isRight = false),
            Options(text = "Domain Name System", isRight = true)
        )
    ),
    Quiz(
        question = "Which of the following is a relational database management system (RDBMS)?",
        options = listOf(
            Options(text = "MongoDB", isRight = false),
            Options(text = "MySQL", isRight = true),
            Options(text = "Redis", isRight = false),
            Options(text = "Cassandra", isRight = false)
        )
    ),
    Quiz(
        question = "What is the purpose of the 'break' statement in programming?",
        options = listOf(
            Options(text = "Ends the program execution", isRight = false),
            Options(text = "Exits a loop or switch statement", isRight = true),
            Options(text = "Prints a line break", isRight = false),
            Options(text = "Declares a break point for debugging", isRight = false)
        )
    ),
    Quiz(
        question = "Which programming language is commonly used for building web servers?",
        options = listOf(
            Options(text = "Java", isRight = false),
            Options(text = "Node.js", isRight = true),
            Options(text = "C#", isRight = false),
            Options(text = "Python", isRight = false)
        )
    ),
    Quiz(
        question = "What is the purpose of the 'docker-compose' command in Docker?",
        options = listOf(
            Options(text = "Create a new Docker image", isRight = false),
            Options(text = "Manage multiple containers as a service", isRight = true),
            Options(text = "Run a Docker container", isRight = false),
            Options(text = "Remove a Docker container", isRight = false)
        )
    ),
    Quiz(
        question = "Which algorithm is commonly used for sorting elements in an array?",
        options = listOf(
            Options(text = "Binary Search", isRight = false),
            Options(text = "Bubble Sort", isRight = false),
            Options(text = "QuickSort", isRight = true),
            Options(text = "Linear Search", isRight = false)
        )
    ),
    Quiz(
        question = "In Python, what is the purpose of the 'virtualenv' tool?",
        options = listOf(
            Options(text = "Create a virtual machine", isRight = false),
            Options(text = "Manage virtual environments for Python projects", isRight = true),
            Options(text = "Install virtual packages", isRight = false),
            Options(text = "Simulate virtual reality environments", isRight = false)
        )
    ),
    Quiz(
        question = "What is the primary purpose of the 'fetch' API in JavaScript?",
        options = listOf(
            Options(text = "Handle form submissions", isRight = false),
            Options(text = "Retrieve resources asynchronously from a network", isRight = true),
            Options(text = "Fetch data from a local database", isRight = false),
            Options(text = "Create a new JavaScript object", isRight = false)
        )
    ),
    Quiz(
        question = "Which design pattern is commonly used to implement a one-to-many relationship?",
        options = listOf(
            Options(text = "Observer Pattern", isRight = false),
            Options(text = "Singleton Pattern", isRight = false),
            Options(text = "Factory Method Pattern", isRight = false),
            Options(text = "Publish-Subscribe Pattern", isRight = true)
        )
    ),
    Quiz(
        question = "In Java, what is the purpose of the 'super' keyword?",
        options = listOf(
            Options(text = "Refer to the current object", isRight = false),
            Options(text = "Invoke a superclass method or constructor", isRight = true),
            Options(text = "Declare a constant value", isRight = false),
            Options(text = "Access a static method", isRight = false)
        )
    ),
    Quiz(
        question = "What is the purpose of the 'async' and 'await' keywords in JavaScript?",
        options = listOf(
            Options(text = "Define asynchronous functions", isRight = true),
            Options(text = "Access external APIs", isRight = false),
            Options(text = "Synchronize multiple threads", isRight = false),
            Options(text = "Create anonymous functions", isRight = false)
        )
    ),
    Quiz(
        question = "Which programming language is known for its use in data analysis and machine learning?",
        options = listOf(
            Options(text = "Java", isRight = false),
            Options(text = "Python", isRight = true),
            Options(text = "C++", isRight = false),
            Options(text = "Ruby", isRight = false)
        )
    )
)